# Cargar dos listas con la misma cantidad de elementos. 
# Luego mezclarlas, cargándolas ordenadas en otra lista.

lista1 = [3, 5, 764, 8]
lista2 = [4, -2, 0, 93]

ordenados = sorted(lista1 + lista2)
print(ordenados)
